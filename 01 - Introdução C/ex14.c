#include <stdio.h>

int main() {

  int a, b, c, d;

  a = 5;
  d = 10;
  b = a * 2 + d * d;
  c = d * d / b;

  printf("Valor de (a) = %i\n", a);
  printf("Valor de (b) = %i\n", b);
  printf("Valor de (c) = %i\n", c);
  printf("Valor de (d) = %i\n", d);

  return 0;

  } 